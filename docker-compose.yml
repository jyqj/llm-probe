services:
  detector:
    build: .
    ports:
      - "${LISTEN_PORT:-8080}:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - detector-data:/app/data
    restart: unless-stopped

volumes:
  detector-data:
